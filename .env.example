# Server-side only — never exposed to the browser (no NEXT_PUBLIC_ prefix).
# Scope: ElevenAgents=Write is sufficient.
ELEVENLABS_API_KEY=

# The agent id from `elevenlabs agents push` (or the dashboard URL).
ELEVENLABS_AGENT_ID=
